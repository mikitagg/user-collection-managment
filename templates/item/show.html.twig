{% extends 'base.html.twig' %}

{% block title %}User Items!{% endblock %}

{% block body %}
{#<style>#}
{#    .items-link {border:solid; margin: 1em auto; background-color: rgba(238,180,217,0.26)}#}
{#</style>#}

<div class="example-wrapper">
    <div class="items">
        {% for item in items %}
                <div class="items-link">
                    {{ item.name }}
                </div>
        {% endfor %}
    </div>
    {% endblock %}
